'use strict';

const schema = require('./mug.schema');
const index = require('./index.schema');


describe('get route', () => {
  it('it gets all of the information from the database when not given an id', () => {
    let map = new schema()
  });
});